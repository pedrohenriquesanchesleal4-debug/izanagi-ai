{
  "name": "@izanagi/cli-next",
  "version": "0.1.0",
  "private": true,
  "description": "Next-generation Izanagi CLI: agent pipeline orchestration over the polyglot cores (Rust gate, Go orchestrator, Python analyzer).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "izanagi-next": "./dist/cli/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli/src/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
